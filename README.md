had some difficulty installing gems:

 gem install forward -- --with-opt-include=/usr/include --with-opt-lib=/lib

need to pass the optional libraries and includes.  not sure why not being picked up by default.

